{
  "name": "sisyphe-hash",
  "version": "0.0.1",
  "description": "Sisyphe module to add an hash for every file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "rimraf": "^2.6.1"
  }
}
